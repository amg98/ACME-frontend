/*import { TestBed } from "@angular/core/testing"

import { FavouriteListsService } from "./favourite-lists.service"*/

describe("FavouriteListsService", () => {
    /*let service: FavouriteListsService

    beforeEach(() => {
        TestBed.configureTestingModule({})
        service = TestBed.inject(FavouriteListsService)
    })

    it("should be created", () => {
        expect(service).toBeTruthy()
    })*/
})
