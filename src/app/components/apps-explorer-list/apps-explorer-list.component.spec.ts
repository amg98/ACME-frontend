// import { ComponentFixture, TestBed } from "@angular/core/testing"

// import { AppsExplorerListComponent } from "./apps-explorer-list.component"

describe("AppsExplorerListComponent", () => {/**
    let component: AppsExplorerListComponent
    let fixture: ComponentFixture<AppsExplorerListComponent>

    beforeEach(async () => {
        await TestBed.configureTestingModule({
            declarations: [ AppsExplorerListComponent ]
        })
            .compileComponents()
    })

    beforeEach(() => {
        fixture = TestBed.createComponent(AppsExplorerListComponent)
        component = fixture.componentInstance
        fixture.detectChanges()
    })

    it("should create", () => {
        expect(component).toBeTruthy()
    }) */
})
