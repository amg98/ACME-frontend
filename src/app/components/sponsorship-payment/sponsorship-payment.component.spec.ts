/*import { ComponentFixture, TestBed } from "@angular/core/testing"

import { SponsorshipPaymentComponent } from "./sponsorship-payment.component"*/

describe("SponsorshipPaymentComponent", () => {
    /*let component: SponsorshipPaymentComponent
    let fixture: ComponentFixture<SponsorshipPaymentComponent>

    beforeEach(async () => {
        await TestBed.configureTestingModule({
            declarations: [ SponsorshipPaymentComponent ]
        })
            .compileComponents()
    })

    beforeEach(() => {
        fixture = TestBed.createComponent(SponsorshipPaymentComponent)
        component = fixture.componentInstance
        fixture.detectChanges()
    })

    it("should create", () => {
        expect(component).toBeTruthy()
    })*/
})
