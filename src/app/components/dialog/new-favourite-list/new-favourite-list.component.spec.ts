/*import { ComponentFixture, TestBed } from "@angular/core/testing"

import { NewFavouriteListComponent } from "./new-favourite-list.component"*/

describe("NewFavouriteListComponent", () => {
    /*let component: NewFavouriteListComponent
    let fixture: ComponentFixture<NewFavouriteListComponent>

    beforeEach(async () => {
        await TestBed.configureTestingModule({
            declarations: [ NewFavouriteListComponent ]
        })
            .compileComponents()
    })

    beforeEach(() => {
        fixture = TestBed.createComponent(NewFavouriteListComponent)
        component = fixture.componentInstance
        fixture.detectChanges()
    })

    it("should create", () => {
        expect(component).toBeTruthy()
    })*/
})
