/*import { ComponentFixture, TestBed } from "@angular/core/testing"

import { EditFavouriteListComponent } from "./edit-favourite-list.component"*/

describe("EditFavouriteListComponent", () => {
    /*let component: EditFavouriteListComponent
    let fixture: ComponentFixture<EditFavouriteListComponent>

    beforeEach(async () => {
        await TestBed.configureTestingModule({
            declarations: [ EditFavouriteListComponent ]
        })
            .compileComponents()
    })

    beforeEach(() => {
        fixture = TestBed.createComponent(EditFavouriteListComponent)
        component = fixture.componentInstance
        fixture.detectChanges()
    })

    it("should create", () => {
        expect(component).toBeTruthy()
    })*/
})
